- /「.*」/g # セリフ用
- /（.*）/g # かっこがき用
- /\[.*\]\(.*\)/g # Markdownリンク用
- /\/\/\/.*/g # コメント用
- /\[https?:.+\]/g # はてなブログのリンク用
- /\(\(.+\)\)/g # はてなブログの注釈用
- /https?:\/\/[^\s]+/g # URL用
- /\[twitter:@.+\]/g # はてな記法
- /\[.+( .+)?\]\n[\s\S]*?\[/.+\]/g # WordPressのハイライト用
- /<a( .+)?>.*</a>/ # リンク用
